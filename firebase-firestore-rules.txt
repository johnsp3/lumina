rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow access to authorized user (jchezik@gmail.com)
    match /{document=**} {
      allow read, write: if request.auth != null && request.auth.token.email == 'jchezik@gmail.com';
    }
  }
} 