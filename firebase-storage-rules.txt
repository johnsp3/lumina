rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Only allow read/write access to jchezik@gmail.com
      allow read, write: if request.auth != null && request.auth.token.email == 'jchezik@gmail.com';
    }
  }
} 